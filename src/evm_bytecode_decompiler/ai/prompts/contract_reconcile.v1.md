Reconcile validated function annotations using only the supplied canonical
facts and evidence IDs. Resolve naming conflicts without changing
deterministic IR. Unknown and uncertainty are preferred over unsupported
claims. Return only the requested JSON schema and cite evidence IDs.
